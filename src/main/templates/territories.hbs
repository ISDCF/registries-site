<!DOCTYPE html>
<html lang="en">

  {{>header}}
  <!-- Main content starts - do not edit above this line -->
    <table id="sorttable" class="table table-small table-hover display">
      <thead class="thead-light">
        <tr>
          <th scope="col">Territory</th>
          <th scope="col">ReleaseTerritory Tag <small>(For DCP Metadata)</small></th>
          <th scope="col">DCNC Code <small>(For CTT)</small></th>
          <th scope="col">Tag Scope</th>
          <th scope="col" class="text-wrap" width="20%">Comments</th>
        </tr>
      </thead>
      <tbody id="searchTable">
        {{#data}}
        <tr id="{{../idType}}-{{rfc5646Tag}}">
          <td><span class="description">{{dcncTerritory}}</span></td>
          <td class="rfc"><a class="anchor" id="{{rfc5646Tag}}" href="#{{rfc5646Tag}}"></a><code>{{tag}}</code></td>
          <td class="code">
            {{#if dcncTag}}
            {{dcncTag}}
            {{else}}
            <em>n/a</em>
            {{/if}}
          </td>
          <td>
            {{#if tagScope}}
            <a target="_blank" href="{{tagScope}}">{{tagScope}}</a>
            {{/if}}
          </td>
          <td class="comment">
            {{#comments}}
                <small>{{this}}</small>
            {{/comments}}
          </td>
        </tr>
        {{/data}}
      </tbody>
    </table>
<!-- Main content ends - do not edit below this line -->
{{>footer}}

</html>